package softuni.jsonexercise.productshop.constants;

public enum messages {
    ;


    public static final String NAME_VALIDATOR = "Last must at least 3 characters !";
    public static final String AGE_VALIDATOR = "Age must be positive digit !";
}
